{% extends 'base.html.twig' %}
{% block content %}

    <div class="slideshow uk-slidenav-position">
        <ul class="uk-slideshow" data-uk-slideshow="{autoplay: true, animation: 'random-fx', height: '450'}">
            {% for image in images %}
            <li><img src={{ image.path|raw }}></li>
            {% endfor %}
        </ul>
    </div>

{% endblock %}